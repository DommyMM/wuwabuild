import React from 'react';

export const BuildsPage: React.FC = () => {
    return (
        <div>
        <h1>Saved Builds</h1>
        {/* Build list will go here */}
        </div>
    );
    };